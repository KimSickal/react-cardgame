import {
	CSSProperties,
} from 'react';

export const styles: {[name: string]: CSSProperties} = {
	hand: {
		display: 'flex',
		flexDirection: 'row',
		justifyContent: 'flex-start',
	},
	hand_card: {
		width: '90',
		height: '160',
	},
};
